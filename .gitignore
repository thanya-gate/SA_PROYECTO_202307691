# Secretos locales (nunca se suben)
.env
.env.local
.env.*.local
.env.cloud
gha-creds-*.json
gha-kubeconfig-*

# Plantillas de variables de entorno (sí se suben)
!.env.example
!.env.cloud.example
AGENTS.md
context/
coverage/
.coverage
coverage.out
.opencode
