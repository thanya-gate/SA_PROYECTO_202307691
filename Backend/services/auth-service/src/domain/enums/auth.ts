export enum SessionStatus {
  ACTIVA = 'ACTIVA',
  EXPIRADA = 'EXPIRADA',
  REVOCADA = 'REVOCADA',
}

export enum TokenType {
  VERIFICACION_CORREO = 'VERIFICACION_CORREO',
  RESET_PASSWORD = 'RESET_PASSWORD',
}

export enum TokenUsageState {
  PENDIENTE = 'PENDIENTE',
  USADO = 'USADO',
}
